#pragma once
#include <iostream>
#include <cmath>
#include <vector>
#include <string>

using namespace std;

class Logica {

    public:
    static int evaluar_intento(const string& candidato,const string& solucion);
    
    static int codificar_intento(const string& candidato);
};  